{{template "base" .}}

{{define "content"}}
  <div class="container">
    <div class="row">
        <div class="col">
          <h1>About Page</h1>
          <p>this is the about page</p>
          <p>this data is from template data: {{index .StringMap "test"}}

          <p>

          {{ if ne (index .StringMap "remote_ip") "" }}
            Your IP address is: {{index .StringMap "remote_ip"}}
          {{ else }}
            I don't know your IP address yet. Please visit <a href="/">Home page</a> first.
          {{ end }}
          </p>
        </div>
    </div>
  </div>
{{end}}